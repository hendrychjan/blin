name: blin
description: An expense manager for students.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  intl: ^0.17.0
  path_provider: ^2.0.11
  get_storage: ^2.0.3
  connectivity_plus: ^2.3.5
  http: ^0.13.4
  jwt_decode: ^0.3.1
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true